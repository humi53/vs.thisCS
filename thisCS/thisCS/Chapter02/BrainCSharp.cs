using System;
using System.Collections.Generic;
using System.Text;
using static System.Console;

namespace thisCS.Chapter02
{
    class BrainCSharp
    {
        //static void Main(string[] args)
        //{
        //    if(args.Length == 0)
        //    {
        //        Console.WriteLine("사용법 : HelloWorld.exe <이름>");
        //        return;
        //    }

        //    WriteLine("Hello, {0}!", args[0]);
        //}
    }
}
/* Hello world 테스트
 * 
 * using 네임스페이스 안에 잇는 클래스를 사용하겠다고 컴파일러에 알림.
 * using static 정적 멤버를 참조하겠다고 선언하기.
 * namespace 성격이나 하는일이 비슷한 클래스, 구조체, 인터페이스, 대리자, 열거 형식등을 하나의 이름 아래에 묶는 일을 한다.
 * class 데이터와 데이터를 처리하는 기능(메소드)으로 이루어짐.
 * CLR 가상머신. 
 * IL CLR이 네이티브 코드로 컴파일후 실행. 다른 언어와같이 쓰이는 중간언어.
 * CLR은 C#이나 기타 언어들을 동작시키는 환경기능 외에, 프로그램 오류(예외)처리를 도와주는 기능, 언어간의 상속지원, COM과 상호운영성지원, 가비지 컬렉션 제공.
 */

